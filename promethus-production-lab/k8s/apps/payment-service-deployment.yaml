apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: apps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: prom-lab/payment-service:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
            - name: metrics
              containerPort: 8000
          env:
            - name: SERVICE_NAME
              value: "payment-service"
            - name: FAILURE_RATE
              value: "0.02"
            - name: LATENCY_SPIKE_PROBABILITY
              value: "0.05"
          readinessProbe:
            httpGet:
              path: /health
              port: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
